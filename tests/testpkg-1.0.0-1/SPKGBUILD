pkgname=testpkg
pkgver=1.0.0
pkgrel=1

sources=(
        "testpkg-1.0.0-1.tar.gz"
        )
md5sums=(
        35d35e5d1eceb70a5395e81f7df6d582
        )

build() {
        cd "$pkgname-$pkgver"
        echo "Building $pkgname-$pkgver-$pkgrel"
}

package() {
        echo "Installing $pkgname-$pkgver-$pkgrel"
        cp -av {usr,etc} $pkgdir
}


