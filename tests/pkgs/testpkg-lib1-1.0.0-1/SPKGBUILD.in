pkgname=testpkg-lib1
pkgver=1.0.0
pkgrel=1

sources=(
        "testpkg-lib1-1.0.0.tar.gz"
        )
md5sums=(
        TESTPKG_MD5SUM 
        )

build() {
        cd "$pkgname-$pkgver"
        echo "build function for: $pkgname-$pkgver-$pkgrel"
}

package() {
        echo "package function for: $pkgname-$pkgver-$pkgrel"
        cp -a usr $pkgdir
}


